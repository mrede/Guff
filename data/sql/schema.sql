CREATE TABLE post (id INT UNSIGNED AUTO_INCREMENT, ip VARCHAR(16) NOT NULL, text VARCHAR(255), accuracy INT, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, latitude DOUBLE(18, 2), longitude DOUBLE(18, 2), PRIMARY KEY(id)) ENGINE = INNODB;
